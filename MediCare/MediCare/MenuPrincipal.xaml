<Window
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MediCare"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Name="fenetre_principal" x:Class="MediCare.MenuPrincipal"
            mc:Ignorable="d" Height="720" Width="1080" WindowStartupLocation="CenterScreen"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            FontFamily="{DynamicResource MaterialDesignFont}" MinWidth="1080" MinHeight="720" ResizeMode="CanMinimize">
    <Grid Background="White">
        <ToolBarTray Margin="0,0,0,598" Background="White">
            <ToolBar Style="{DynamicResource MaterialDesignToolBar}" ClipToBounds="False" Margin="0" Background="White" Height="95.6">
                <ToolBar.Header>
                    <Image Height="84" Width="294" Source="ressources/images/medicare_white.PNG" Stretch="Fill" VerticalAlignment="Center"/>
                </ToolBar.Header>
                <Button x:Name="Home" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Foreground="Black" Width="40" Height="40" Background="{x:Null}" BorderBrush="{x:Null}" Click="Home_Click">
                    <materialDesign:PackIcon Kind="Home" Foreground="#FF42919E" Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Button.ToolTip>
                        <TextBlock>Acceuil</TextBlock>
                    </Button.ToolTip>
                </Button>
                <Separator />
                <Button x:Name="Compte" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Foreground="Black" Width="40" Height="40" Background="{x:Null}" BorderBrush="{x:Null}" Click="Compte_Click">
                    <materialDesign:PackIcon Kind="Account" Foreground="#FF42919E" Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Button.ToolTip>
                        <TextBlock>Mon Compte</TextBlock>
                    </Button.ToolTip>
                </Button>
                <Separator />
                <Button x:Name="Consultation" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Foreground="Black" Width="40" Height="40" Background="{x:Null}" BorderBrush="{x:Null}" Click="Consultation_Click">
                    <materialDesign:PackIcon Kind="MedicalBag" Foreground="#FF42919E" Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Button.ToolTip>
                        <TextBlock>Consultation</TextBlock>
                    </Button.ToolTip>
                </Button>
                <Separator />
                <Button x:Name="Agenda" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Foreground="Black" Width="50" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" Click="Agenda_Click">
                    <materialDesign:PackIcon Kind="Calendar" Foreground="#FF42919E" Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Button.ToolTip>
                        <TextBlock>Agenda</TextBlock>
                    </Button.ToolTip>
                </Button>
                <Separator />
                <Button x:Name="AjtMedic" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" Foreground="Black" Width="50" Height="50" Background="{x:Null}" BorderBrush="{x:Null}" Click="AjtMedic_Click">
                    <materialDesign:PackIcon Kind="DatabasePlus" Foreground="#FF42919E" Width="35" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Button.ToolTip>
                        <TextBlock>Ajouter un médicament à la base de données</TextBlock>
                    </Button.ToolTip>
                </Button>
                <Separator />
                <StackPanel x:Name="SearchPanel" materialDesign:RippleAssist.IsCentered="True" Width="316" Margin="0" Orientation="Horizontal">
                    <materialDesign:ColorZone Mode="Standard" Padding="8 4 8 4" CornerRadius="2" Panel.ZIndex="1" Margin="16,27,0,27" materialDesign:ShadowAssist.ShadowDepth="Depth1" Width="250" VerticalAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button Style="{DynamicResource MaterialDesignToolButton}" Click="Button_Click">
                                <materialDesign:PackIcon Kind="Magnify" Opacity=".56" />
                            </Button>
                            <ComboBox x:Name="element" Grid.Column="1" Margin="8 0 0 0" materialDesign:HintAssist.Hint="Rechercher un élément..." materialDesign:TextFieldAssist.DecorationVisibility="Hidden" BorderThickness="0" MinWidth="200" VerticalContentAlignment="Bottom" IsEditable="True" TextBoxBase.TextChanged="element_TextChanged" IsTextSearchEnabled="False" GotFocus="element_GotFocus" PreviewKeyDown="element_PreviewKeyDown" />
                        </Grid>
                    </materialDesign:ColorZone>
                    <Button Style="{DynamicResource MaterialDesignToolForegroundButton}" Margin="8,31,0,31" Panel.ZIndex="0" Width="27">
                        <materialDesign:PackIcon Kind="Send" Foreground="#FF42919E" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Button>
                </StackPanel>
                <Separator />
                <StackPanel x:Name="OptionPopUp" Orientation="Horizontal" Height="78" Margin="0,6,0,5" Width="51">
                    <materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" StaysOpen="False" Margin="10" Foreground="#FF42919E" Width="27">
                        <StackPanel Width="150">
                            <Button x:Name="SettingBTN" Content="Settings"/>
                            <Button x:Name="HelpBTN" Content="Help"/>
                            <Separator/>
                            <Button x:Name="LogoutBTN" Content="Logout" Click="LogoutBTN_Click" />
                        </StackPanel>
                    </materialDesign:PopupBox>
                </StackPanel>
                <Separator />
            </ToolBar>
        </ToolBarTray>
        <StackPanel HorizontalAlignment="Right" Height="40" Margin="0" VerticalAlignment="Bottom" Width="150">
            <Grid Height="50" VerticalAlignment="Center" Width="150">
                <Button x:Name="Twitter" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Width="50" Height="Auto" Margin="0" Foreground="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Top" >
                    <materialDesign:PackIcon Kind="Twitter" Width="40" Height="40" Foreground="#FF42919E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="Facebook" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Width="50" Height="Auto" Foreground="{x:Null}" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Top" >
                    <materialDesign:PackIcon Kind="Facebook" Width="40" Height="40" Foreground="#FF42919E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="Info" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Width="50" Height="Auto" Margin="0" Foreground="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="40" Height="40" Foreground="#FF42919E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </Grid>
        </StackPanel>
        <Grid x:Name="SelectionGrid" Margin="0,85,0,56">
        </Grid>
        <materialDesign:DrawerHost BorderThickness="2" Margin="0" d:IsHidden="True">
            <materialDesign:DrawerHost.RightDrawerContent>
                <StackPanel Margin="70">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Text="Notification" FontFamily="Century Gothic" Foreground="#FF1D3E44"></TextBlock>
                    <Button Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}" CommandParameter="{x:Static Dock.Right}" Margin="4" HorizontalAlignment="Center" Style="{DynamicResource MaterialDesignFlatButton}" Foreground="#FF42919E" FontFamily="Century Gothic">
                        Fermer
                    </Button>
                    <Button  Margin="4" HorizontalAlignment="Center" Style="{DynamicResource MaterialDesignFlatButton}" Foreground="#FF42919E" FontFamily="Century Gothic" Click="EffacerNotif_Click">
                        Effacer
                    </Button>
                    <ListView x:Name="ListeNotif">
                    </ListView>
                </StackPanel>
            </materialDesign:DrawerHost.RightDrawerContent>
            <materialDesign:Badged x:Name="NotificationBadge" Badge="0" HorizontalAlignment="Right" BadgeBackground="Green" BadgeForeground="White" VerticalAlignment="Top" Margin="0,16,8,0" Height="60" Width="55">
                <Button  Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                    CommandParameter="{x:Static Dock.Right}" x:Name="ClocheNotif" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Margin="0" Foreground="Black" Width="Auto" Height="Auto" Background="{x:Null}" BorderBrush="{x:Null}">
                    <materialDesign:PackIcon Kind="Bell" Foreground="#FF42919E" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"  />
                </Button>
            </materialDesign:Badged>
        </materialDesign:DrawerHost>
        <Button x:Name="Settings" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Width="50" Height="Auto" Margin="0" Foreground="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Bottom" >
            <materialDesign:PackIcon Kind="Settings" Width="40" Height="40" Foreground="#FF42919E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Button>
        <Separator Height="2" Margin="0,0,0,53" VerticalAlignment="Bottom" Width="1072" Background="#FF42919E"/>
    </Grid>
</Window>
